# Voice Bot Agent Dockerfile (for VoiceBotContext integration)
FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci --only=production && npm cache clean --force

# Copy agent files
COPY agents/voiceBotAgent.js ./agents/
COPY agents/runner.js ./
COPY services/ ./services/
COPY models/ ./models/
COPY config/ ./config/

# Create directories
RUN mkdir -p logs

# Create non-root user
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nodejs -u 1001

RUN chown -R nodejs:nodejs /app
USER nodejs

HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD node healthcheck.js

CMD ["node", "runner.js", "voice-bot-processor"]
